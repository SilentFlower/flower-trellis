{
  "heading": "Per-turn prompt injection",
  "missing": "skip",
  "lines": [
    "#-------------------------------------------------------------------------------",
    "# Per-turn prompt injection",
    "#-------------------------------------------------------------------------------",
    "# Escape hatch for the per-turn <workflow-state> breadcrumb. When a user",
    "# prompt contains the skip keyword as a standalone word (case-insensitive,",
    "# word-boundary match — \"no-trellisfoo\" does NOT count), the breadcrumb is",
    "# skipped for that turn only. Does not affect SessionStart or sub-agent",
    "# context injection.",
    "#",
    "# Codex and Claude suppress unchanged workflow-state bodies. A short heartbeat",
    "# is emitted after the configured number of unchanged user prompts. The default",
    "# is 5; 0 disables heartbeats; invalid or negative values fall back to 5.",
    "# State changes and startup / clear / compact still emit the full state.",
    "#",
    "# prompt_injection:",
    "#   skip_keyword: \"no-trellis\"   # \"\" disables the escape hatch entirely",
    "#   heartbeat_turns: 5          # 0 disables unchanged-state heartbeats",
    ""
  ]
}
