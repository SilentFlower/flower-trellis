# Release Operations

## Conclusion

Needs human review.

本任务已提交并推送代码与快照变更，但是否立即对外发布 npm / GitHub Release 需要人工决定。若要让下游用户安装到新的 `spec_router.py` 与 workflow 触发文案，必须按 flower-trellis 发版 SOP 执行 beta 或稳定版 release。

## Evidence Checked

- task.json
- prd.md
- design.md
- implement.md
- implement.jsonl
- check.jsonl
- release.md：此前不存在
- git commits / changed files:
  - `39a0933 fix(0.6): 优化 spec_router 知识发现` in `vendor/skill-garden`
  - `ee59673 fix(enhancements): 优化 spec_router 知识发现`
  - `a60b2ec chore(task): update spec-router-knowledge-discovery-optimization push snapshot`

## Drift Check

Missing release.md. Current task changed packaged enhancement assets and skill-garden 0.6 templates, so release timing needs human review.

## SQL Changes

None

## Configuration Changes

- `vendor/skill-garden/.trellis/0.6/overrides/workflow.md`
- `vendor/skill-garden/.trellis/0.6/overrides/workflow-states/*.md`
- `enhancements/0.6/overrides/workflow.md`
- `enhancements/0.6/overrides/workflow-states/*.md`
- `.trellis/workflow.md` dogfood copy

These are Trellis workflow/template behavior changes. Downstream projects receive them only after installing/updating the packaged enhancement.

## Batch / Deployment Scripts / Data Repair

None

## External Systems / Dependent Platforms

- npm package `flower-trellis` if this change should be distributed to users.
- GitHub Release generated by `.github/workflows/release.yml` after pushing a release tag.
- `vendor/skill-garden` has already been pushed to `beta` at `39a0933`; `flower-trellis` references that submodule commit and includes the synchronized `enhancements/0.6` snapshot.

## Release Order

1. Confirm whether to ship this change as beta or stable.
2. Follow `.trellis/spec/flower-trellis/cli/release-and-publishing.md`:
   - run the matching `npm run release:dry...` command first;
   - show the generated version and CHANGELOG section;
   - wait for explicit confirmation;
   - run `npm run sync` before the real release;
   - ensure `scripts/check-snapshot.mjs` passes;
   - run the real `npm run release...`;
   - push the tag with `git push --follow-tags origin <branch>`.

## Rollback Notes

Rollback code only if not released. If released, publish a follow-up version that reverts the `spec_router.py` / workflow behavior and the synchronized `enhancements/0.6` snapshot.

## Post-release Verification

- Verify `npm view flower-trellis version` or `npm view flower-trellis@beta version` according to the chosen channel.
- Install/update a temporary Trellis target and verify `.trellis/scripts/spec_router.py` contains `confidence` output and the new token/index matching behavior.
- Verify the workflow hub no longer uses `procedural or high-impact actions` as the project knowledge discovery trigger.
